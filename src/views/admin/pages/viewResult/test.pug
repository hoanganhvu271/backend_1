extends ../../layout/default.pug
include ../../mixins/pagination.pug
include ../../mixins/test.pug


block main
  br
  .search-wrapper.d-flex.align-items-center
    input(type="text" class="form-control search-box-test" placeholder="Something clever..")
    button.btn.btn-light Lọc
  br
  h4 Danh sách bài thi
  .test-list.overflow-auto
    each test in tests
      +testMixin(test)
  +pagination(5)