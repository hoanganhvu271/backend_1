doctype html
html(lang="en")
  head
    //- Required meta tags
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
    link(href="https://fonts.googleapis.com/css?family=Roboto:300,400&display=swap", rel="stylesheet")
    link(rel="stylesheet", href="/css/owl.carousel.min.css" )
    //- Bootstrap CSS
    link(rel="stylesheet", href="/css/bootstrap.min.css")
    //- Style
    link(rel="stylesheet", href="/css/loginUser.css")
    if data && data.title
        title= data.title
    else
         title Đăng nhập

  body
    .content
      .container
        .row
          .col-md-6
            img(src="/static/images/undraw_remotely_2j6y.svg", alt="Image", class="img-fluid")
          .col-md-6.contents
            .row.justify-content-center
              .col-md-8
                .mb-4
                  
                h1.text Quizz App
                form(action="checkLoginUser", method="post")
                  .form-group.first
                    
                    input(type="text", class="form-control", id="username", name = "msv", placeholder="Mã sinh viên" Required)

                  .form-group.last.mb-4
                    
                    input(type="password", class="form-control", id="password", name ="password", placeholder="Mật khẩu" Required)

                  .d-flex.mb-5.align-items-center
                    label.control.control--checkbox.mb-0
                      span.caption Ghi nhớ
                      input(type="checkbox", checked="checked")
                      .control__indicator

                    span.ml-auto
                      a.forgot-pass(href="forgotPassword") Quên mật khẩu
                  
                  input(type="submit", value="Đăng Nhập", class="btn btn-block", style="background-color: #c32b2b; color:white;")
                  p.register Chưa có tài khoản, 
                   a.register(href="register") Đăng ký
                  br
                  if data && data.message
                   p.aler= data.message
