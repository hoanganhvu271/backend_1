extends ../../layout/default.pug
include ../../mixins/pagination.pug

block main
  .row.align-items-center.justify-content-between.mb-6
    .search-wrapper.d-flex.align-items-left.ml-2
        input( type="text" class="form-control search-box-2" tim-kiem-stu placeholder="---Tìm kiếm---" value=keyword)
        button.btn.btn-light(btn-loc-stu) Lọc
  hr
  h5 Danh sách bài thi đã làm
  table(class="table table-striped")
    thead
      tr
        th STT
        th Tên bài thi
        th Trạng thái
        th Điểm
        th Chi tiết
    tbody
        each test, index in testList
            tr
              td #{index + 1}
              td #{test.TenBaithi}
              td #{test.TrangThai}
              td #{test['Results.Diem']}
              td
                a.btn.btn-primary(btn-detail-test-with-idResult idResult=test['Results.MaKetQua']) Xem chi tiết
  +pagination(pagination)